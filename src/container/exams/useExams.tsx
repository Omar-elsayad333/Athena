import { useState } from 'react'

const useExams = () => {
    const [ loading ] = useState<boolean>(false)

    return (
        {
            data: {
                
            },
            states: {
                loading
            },
            actions: {
                
            },
            dialogs: {

            }
        }
    );
}
 
export default useExams;