

// MUI
import Box from "@mui/material/Box";

type Props = {
    data: any;
    states: any;
    actions: any;
    dialogs: any;
}

const ReviewExamC: React.FC<Props> = ({ data, states, actions, dialogs }) => {
    return (
        <Box>

        </Box>
    );
}
 
export default ReviewExamC;